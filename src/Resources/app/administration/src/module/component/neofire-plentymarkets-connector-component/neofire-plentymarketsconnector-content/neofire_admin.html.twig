<sw-page class="swag-bundle-list">
    <template slot="content">
      <iframe :src="apiUserData" v-if="apiUserData" width="100%" height="99%" frameborder="0"></iframe>
    </template>

    <template slot="smart-bar-actions">
        <sw-button variant="primary" v-if="apiUserData" @click="openNewTab">
            in neuem Fenster öffnen
        </sw-button>
    </template>

</sw-page>
